<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.itlyc.mapper.IndustryMapper">
    <select id="queryIndustryList" resultType="com.itlyc.sys.entity.CommonIndustry">
        select * from sys_common_industry
        <where>
            <choose>
                <when test="parentId == null or parentId == ''">
                    parent_id is null
                </when>
                <otherwise>
                    parent_id = #{parentId}
                </otherwise>
            </choose>
        </where>

    </select>
</mapper>
